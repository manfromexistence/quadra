In this project at the home page of our dashboard there is an AI chat and in that chat there is a model picker. I don't need the model picker but there is a Groq API key in this project. Please use the Groq API key with the best detailed model and use them by default. No need to show the model details at all in the UI but make sure it works. Currently when we are typing anything to the model, it's not working and showing the result so please make sure it works correctly with our Groq API and returns an answer.

Now here is very critical stuff. For EDMAs and our authentication we are using a real database with Tutsu CLI and Tutsu database with beta dot and result, right? Keep using that. For the mock data, where we are using the mock data, it is facing it again and again. Can't we use React query to keep it in the cache so that it doesn't face it again and again and waste time?
